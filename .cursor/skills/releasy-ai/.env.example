# Copy this file to ".env" in this same folder and fill in a write-scoped Azure DevOps PAT.
# This is a dedicated credential for the releasy-ai skill, separate from the repo's own
# dev.env (see AGENTS.md) - never commit the real ".env" file (it is git-ignored).
AZURE_DEVOPS_PAT=
